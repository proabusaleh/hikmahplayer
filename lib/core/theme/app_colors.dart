import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Player colors
  static const Color playerBackground = Color(0xFF000000);
  static const Color playerControls = Color(0xFFFFFFFF);
  static const Color seekBarPlayed = Color(0xFF6C5CE7);
  static const Color seekBarRemaining = Color(0x40FFFFFF);
}
