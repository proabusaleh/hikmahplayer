name: hikmahplayer
description: "Play with Wisdom — A beautiful media player"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: '>=3.29.0'

dependencies:
  flutter:
    sdk: flutter

  # ─── STATE MANAGEMENT ───
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # ─── NAVIGATION ───
  go_router: ^14.2.0

  # ─── MEDIA PLAYBACK ───
  media_kit: ^1.1.10+1              # Cross-platform media engine (libmpv)
  media_kit_video: ^1.2.4           # Video rendering widget
  media_kit_libs_video: ^1.0.4      # Native libs (Android/iOS/Windows)

  # ─── AUDIO SESSION ───
  audio_session: ^0.1.21            # Audio focus, interruptions
  audio_service: ^0.18.13           # Background audio + notifications

  # ─── DATABASE ───
  drift: ^2.18.0                    # SQLite ORM (type-safe)
  sqlite3_flutter_libs: ^0.5.21     # SQLite native libs

  # ─── LOCAL STORAGE ───
  shared_preferences: ^2.2.3        # Key-value storage
  path_provider: ^2.1.3             # App directories
  path: ^1.9.0                      # Path manipulation

  # ═══════════ PERMISSIONS ═══════════
  permission_handler: ^11.3.1       # Runtime permissions

  # ─── UI COMPONENTS ───
  google_fonts: ^6.2.1              # Custom typography
  flutter_svg: ^2.0.10+1            # SVG rendering
  lottie: ^3.1.2                    # Lottie animations (onboarding)
  cached_network_image: ^3.3.1      # Image caching
  shimmer: ^3.0.0                   # Loading placeholders
  flutter_staggered_grid_view: ^0.7.0  # Masonry grid (videos)
  sliver_tools: ^0.2.12             # Advanced slivers
  animations: ^2.0.11               # Material motion

  # ─── UTILITIES ───
  intl: ^0.20.2                     # Date/number formatting
  uuid: ^4.4.0                      # Unique IDs
  equatable: ^2.0.5                 # Value equality
  collection: ^1.18.0               # List utilities
  rxdart: ^0.27.7                   # Reactive streams
  logger: ^2.3.0                    # Pretty logging

  # ─── CODE GENERATION (Annotations) ───
  freezed_annotation: ^2.4.1        # Immutable models
  json_annotation: ^4.9.0           # JSON serialization

  # ─── PLATFORM SPECIFIC ───
  wakelock_plus: ^1.2.5             # Keep screen on during playback
  screen_brightness: ^2.1.11        # Brightness control (gestures)
  volume_controller: ^3.6.0         # Volume control (gestures)

  # ─── RETAINED (imported by existing code) ───
  crypto: ^3.0.7                    # Content hashing & duplicate detection
  dio: ^5.4.0                       # HTTP client
  dynamic_color: ^2.1.0             # Material You palettes
  flutter_animate: ^4.5.0           # Animations
  hive_flutter: ^1.1.0              # Fast cache
  package_info_plus: ^8.0.0         # Version display (About screen)
  web_socket_channel: ^3.0.0        # Real-time sync

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

  # ─── CODE GENERATION ───
  build_runner: ^2.4.11
  riverpod_generator: ^2.4.0
  freezed: ^2.5.2
  json_serializable: ^6.8.0
  drift_dev: ^2.18.0

  # ─── TESTING ───
  mocktail: ^1.0.3
  # NOTE: 'riverpod_test' omitted — its transitive 'test' constraint is
  # incompatible with freezed/build under Flutter 3.44. Test providers
  # directly with ProviderContainer instead.

  # ─── ICONS ───
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.4.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application.
  uses-material-design: true

  # To add assets to your application, add an assets section here.
  #
  # To add custom fonts to your application, add a fonts section here.
